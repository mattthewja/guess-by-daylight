/**
 * This is a separate from the GuidePage as it will serve as the object
 * a user can go to when they are in a game. 
 * 
 * NOTE: This is a WIP and is scheduled to be reconsidered.
 * IF seeding logic is implemented instead, we can now
 * save the seed of the user into session or local storage
 * and this guidebook is useless as we can just have the user 
 * go to and leave the guide page.
 * 
 * Alternatively, the guide page can be deemed unnecessary instead
 * and the guidebook is pulled up every time the user wants to read
 * as an absolute overlay
 */